# The game engine and the random / rule-based / MCTS agents need only numpy.
numpy>=1.26.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
websockets>=12.0

# RL training and the trained-policy agent
torch>=2.1.0
gymnasium>=0.29.0

# Development / testing
pytest>=7.4.0
httpx>=0.27.0        # required by fastapi.testclient
